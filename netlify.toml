[build]
  publish = "."
  functions = "netlify/functions"

# Mantém a mesma URL da Vercel (/api/enviar-lead) também na Netlify.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
